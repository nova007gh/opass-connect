import {useEffect,useState} from 'react';import {View,Text,FlatList,StyleSheet} from 'react-native';import {api} from '../lib/api';
export default function Events(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api('/events').then(setData).catch(()=>{})},[]);return <View style={s.root}><Text style={s.title}>Events</Text><FlatList data={data} keyExtractor={x=>x.id} renderItem={({item})=><View style={s.card}><Text style={s.name}>{item.title}</Text><Text>{new Date(item.startsAt).toLocaleString()}</Text><Text>{item.venue||'Online / TBA'}</Text></View>}/></View>}const s=StyleSheet.create({root:{flex:1,padding:18,backgroundColor:'#f7f9fc'},title:{fontSize:30,fontWeight:'900',color:'#0B2D6B',marginBottom:16},card:{backgroundColor:'#fff',padding:18,borderRadius:18,marginBottom:12},name:{fontSize:19,fontWeight:'900'}})
